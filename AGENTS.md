# AGENTS.md

## Spec-driven development

`./specs` contains language-agnostic project specifications. In principle, any AI/agent and any programming language can be used to produce an equivalent product from the spec.

Therefore: for all future changes, first consider whether the change affects product behavior. If it does, update the spec first, then derive product changes from the spec.
